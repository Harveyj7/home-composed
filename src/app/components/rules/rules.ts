import { Component } from '@angular/core';
import {Nav} from '../nav/nav';

@Component({
  selector: 'app-rules',
  imports: [
    Nav
  ],
  templateUrl: './rules.html',
  styleUrl: './rules.scss',
})
export class Rules {

}
